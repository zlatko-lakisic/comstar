/// Reach product identity for AO session overlays (AO ≥ 2.0 / Reach ≥ 0.5).
///
/// Passed as [ReachConnectionConfig.appId]. ao_reach normalizes to lowercase
/// (`ComStar` → `comstar`).
library;

const String kComstarReachAppId = 'ComStar';
