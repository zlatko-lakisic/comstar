name: comstar_channel
description: COMSTAR text channel — Telegram ingress on Ada, sharing identity with the terminal.
publish_to: none
version: 0.1.0

environment:
  sdk: ^3.5.0

dependencies:
  ao_reach:
    path: ../vendor/agentic-orchestration-reach
  args: ^2.6.0
  http: ^1.2.2
  path: ^1.9.0
  yaml: ^3.1.3

dev_dependencies:
  lints: ^5.0.0
  test: ^1.25.0
