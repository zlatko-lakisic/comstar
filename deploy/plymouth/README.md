# COMSTAR Plymouth theme

Early-boot splash for Raspberry Pi OS. Install once:

```bash
sudo bash scripts/install-plymouth-comstar.sh
# or from Mac after deploy:
make plymouth
```

Regenerate PNGs: `python3 generate_assets.py` (stdlib only).
