# vision MCP

Implemented in milestone M5 (`mcp/vision_mcp/`).

This hosted MCP wraps CodeProject.AI and exposes `who_is_present`,
`describe_view`, and `check_camera` to the orchestrator. See
`docs/CONTRACTS.md` §5.
