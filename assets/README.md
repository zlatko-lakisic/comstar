# assets

`comstar.glb` is gitignored (large binary). For TalkingHead experiments:

```bash
curl -fsSL -o assets/comstar.glb \
  https://raw.githubusercontent.com/met4citizen/TalkingHead/main/avatars/brunette.glb
```

Shipping kiosk avatar is the SVG emblem in `terminal/kiosk/` — not GLB lip-sync.
